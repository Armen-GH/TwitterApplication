{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "migrate": "dotenv -e .env -- node-pg-migrate -c migration-config.js up"
  },
  "dependencies": {
    "body-parser": "^2.2.0",
    "cookie-parser": "^1.4.7",
    "dotenv": "^17.2.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "dotenv-cli": "^8.0.0",
    "node-pg-migrate": "^8.0.3"
  }
}
